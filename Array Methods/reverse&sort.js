// ---------------------reverse method------------------
// it reverse the order of the arr elements from last to first and vice versa
// and it also overrides the original array

// const num = [1, 2, 3, 4, 5];
// const result = num.reverse();
// console.log(result);

// -----------------------sort---------------------
// this method sorts as a string in alphabetical and ascending order
// Create an Array
const fruits = ["Banana", "Orange", "Apple", "Mango"];

// Sort the Array
const result = fruits.reverse();
console.log(result);
// // Reverse the array
// fruits.reverse()
