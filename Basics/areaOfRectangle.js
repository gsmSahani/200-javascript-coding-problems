// write a function to calculate the area of reactangle given width and height
// formul a=w*l
function calculateArea(w, l) {
  return w * l;
}
console.log(`Area of rectangle is ${calculateArea(10, 20)}`);
